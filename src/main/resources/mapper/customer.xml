<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.my.shop.customer.CustomerRepository">

    <insert id="saveCustomer">
        INSERT IGNORE INTO customer
        (customer_name, cust_num, customer_tel, cust_address)
        VALUES
        (#{customerName}, #{custNum}, #{customerTel}, #{custAddress});
    </insert>

    <select id="selectCustomer" resultType="com.my.shop.customer.dto.CustomerResponseDTO$Customer">
        SELECT
            *
        FROM
            customer
        WHERE
            customer_name like concat('%', #{customerName},'%');
    </select>



</mapper>